$pattern = '"similarity":\s*\[.*?\]'

# Remove lines that match the pattern
$filteredContent = $jsonContent -replace $pattern, '' | Where-Object { $_ -notmatch '"\s*:\s*null\s*,' -and $_ -notmatch '"\s*:\s*null\s*}' }
